const fonts = {
  montserrat: 'Montserrat, sans-serif'
};

const colors = {
  white: '#fff',
  cream: '#c4bda1',
  tuatara: '#32312C'
};

const screen = {
  sm: 576,
  md: 768,
  lg: 992,
  xl: 1200
};

const theme = {
  fonts,
  colors,
  screen,
  brandPrimary: '#2CA4AB',
  textMuted: '#d1d0d0',
  textColor: '#939598'
};

export default theme;
